{
  "name": "time-to-do",
  "version": "1.0.0",
  "main": "index.html",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "parcel index.html -p 2323",
    "watch": "parcel watch index.html",
    "build": "parcel build index.html --public-url /time-to-do/",
    "deploy": "gh-pages -d dist",
    "predeploy": "rm -rf dist/ && yarn run build"
  },
  "homepage": "https://yeseulo.github.io/time-to-do",
  "devDependencies": {
    "cssnano": "^4.1.10",
    "node-sass": "^4.14.1"
  },
  "dependencies": {
    "gh-pages": "^3.1.0"
  }
}
